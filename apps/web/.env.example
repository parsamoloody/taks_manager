# Server-only URL used by SSR and API routes. Do not expose it with NEXT_PUBLIC_.
API_URL=http://localhost:4000

# Use a long, random value in every deployed environment.
SESSION_SECRET=replace-with-a-long-random-secret

# Optional backend request timeout in milliseconds.
API_TIMEOUT_MS=10000

# NODE_ENV instance
APP_ENV=development